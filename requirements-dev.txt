# Development-only dependencies: testing and code-quality tooling.
# Install alongside the production requirements when developing locally:
#
#     pip install -r requirements.txt -r requirements-dev.txt
#
# These are intentionally NOT installed in the production Docker image
# (see Dockerfile) -- a running service has no need for pytest or a linter.

-r requirements.txt

# ===== Testing =====
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<0.25.0
pytest-cov>=5.0.0,<6.0.0
httpx>=0.27.0,<0.28.0

# ===== Code Quality =====
black>=24.10.0,<25.0.0
ruff>=0.7.0,<0.8.0
mypy>=1.13.0,<2.0.0
